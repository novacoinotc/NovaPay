# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm turbo build --filter=@novapay/worker"]

[start]
cmd = "node apps/worker/dist/index.js"
