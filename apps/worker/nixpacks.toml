# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["cd ../.. && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd ../.. && pnpm turbo build --filter=@novapay/worker"]

[start]
cmd = "node dist/index.js"
